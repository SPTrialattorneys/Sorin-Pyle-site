---
layout: layouts/blog-post.html
title: Legal Insights Blog
description: Expert legal insights and updates from Sorin & Pyle criminal defense attorneys in West Michigan
author: Sorin & Pyle
date: 2025-01-01
eleventyExcludeFromCollections: true
---

<div class="blog-archive">
    <p class="text-align-center sub-headline margin-bottom-xl">
        Expert legal insights, case updates, and criminal defense news from our experienced attorneys serving West Michigan.
    </p>

    {% if collections.posts.length > 0 %}
    <div class="blog-posts-grid">
        {% for post in collections.posts %}
        <article class="card_blog-post margin-bottom-lg">
            <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
            <p class="blog-post-meta">
                By {{ post.data.author }} | <time datetime="{{ post.data.date | htmlDateString }}">{{ post.data.date | readableDate }}</time>
            </p>
            <p>{{ post.data.description }}</p>
            <a href="{{ post.url }}" class="button-secondary">Read More →</a>
        </article>
        {% endfor %}
    </div>
    {% else %}
    <div class="text-align-center margin-top-xl">
        <p>No blog posts yet. Check back soon for legal insights and updates!</p>
    </div>
    {% endif %}
</div>
